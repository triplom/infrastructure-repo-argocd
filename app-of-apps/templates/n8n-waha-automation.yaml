{{- if .Values.n8nWahaAutomation.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: n8n-waha-automation
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: applications
  source:
    repoURL: {{ .Values.n8nWahaAutomation.repoURL }}
    targetRevision: {{ .Values.n8nWahaAutomation.targetRevision }}
    path: k8s
  destination:
    server: https://kubernetes.default.svc
    namespace: automation
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
{{- end }}
