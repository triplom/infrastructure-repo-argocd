apiVersion: apps/v1
kind: Deployment
metadata:
  name: external-app1-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: external-app1
  template:
    metadata:
      labels:
        app: external-app1
    spec:
      containers:
      - name: external-app
        image: ghcr.io/triplom/external-app1:latest
        ports:
        - containerPort: 8080
        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"
          limits:
            memory: "128Mi"
            cpu: "500m"
